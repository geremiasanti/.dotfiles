backend = "glx";
vsync = true;

blur: {
  method = "gaussian";
  size = 20;
  deviation = 20.0;
};

corner-radius = 20;    # tweak 0–20, higher = rounder
round-borders = 1;     # 0 = none, 1 = all windows, 2 = only top, etc.

round-borders-exclude = [
  "class_g = 'alacritty'",
  "class_g = 'i3bar'",
  "class_g = 'Polybar'",
  "class_g = 'Rofi'",
  "class_g = 'dmenu'",
  "class_g = 'slop'"
];

rounded-corners-exclude = [
  "window_type = 'dock'",
  "window_type = 'desktop'"
];
